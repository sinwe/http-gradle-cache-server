rootProject.name = 'http-cache-server'

buildCache {
    local {
        enabled = false
    }
    remote(HttpBuildCache) {
        url = "http://cache1.winar.to:32500/"
        enabled = true
        push = true
    }
}